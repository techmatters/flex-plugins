import { styled } from '@twilio/flex-ui';

import { StyledNextStepButton } from '../HrmStyles';

export const LoadConversationButton = styled(StyledNextStepButton)`
  display: inline-flex;
`;
LoadConversationButton.displayName = 'LoadConversationButton';
