terraform {
  required_providers {
    twilio = {
      source  = "twilio/twilio"
      version = "0.17.0"
    }

    awscc = {
      source  = "hashicorp/awscc"
      version = "0.51.0"
    }
  }

  backend "local" {
    path = "${local_state_path}"
  }
}
